import React, {Component} from 'react';
//import * as ingredientActions from '../../interface/ingredientInterface.js'

class Orders extends Component {
  
  constructor(props) {
    super(props)
    this.state = {
    };
  }

  render() {
    return (
      <main>This is the Orders Page</main>
    );
  }
}

export default Orders