import React, {Component} from 'react';
//import * as ingredientActions from '../../interface/ingredientInterface.js'

class Inventory extends Component {
  
  constructor(props) {
    super(props)
    this.state = {
    };
  }

  render() {
    return (
    	<main>This is the Inventory page</main>
    );
  }
}

export default Inventory